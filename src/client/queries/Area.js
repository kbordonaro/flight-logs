import React, { Component } from 'react';

class Area extends Component {
  state = { };

  render() {
    return (
      <div>
        Area
      </div>
    );
  }
}

Area.defaultProps = {
};

Area.propTypes = {
};

export default Area;